# Optional: only used if you deploy via Netlify instead of GitHub Pages.
# For the fastest one-off deploy you can also just drag the `dist` folder
# onto https://app.netlify.com/drop after running `npm run build`.
[build]
  command = "npm run build"
  publish = "dist"
